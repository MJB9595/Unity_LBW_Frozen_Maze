{
  "action": "execute",
  "code": "var tower = UnityEditor.EditorUtility.InstanceIDToObject(61768) as GameObject; var es = tower.transform.Find(\"EdgeSearch\"); var rs = es.GetComponent<RaySearch>(); var col = tower.GetComponent<Collider>(); var b = col.bounds; Vector3 outside = new Vector3(b.center.x, 25.5f, b.max.z + 3f); rs.cornerPoints.Clear(); rs.meshPoints.Clear(); rs.mergeMode = WallMerge.WallMergeMode.Current; rs.DoPointsFromPlayer(outside, new Vector3(0,0,-1f)); var cp = rs.cornerPoints; int N = cp.Count; float distanceToTurn = 1f; var l1 = new GameObject(\"_l1\").transform; var l2 = new GameObject(\"_l2\").transform; System.Func<int,bool,Vector3,Vector3,Vector3,Vector3> getPivot = (currentIndex, right, decalPos, savedNormal, dummy) => { Vector3 pos = cp[currentIndex].position; int prev = (currentIndex-1>-1)?currentIndex-1:N-1; int next=(currentIndex+1<N)?currentIndex+1:0; bool origin = Vector3.Distance(decalPos, (right? cp[prev].position : cp[currentIndex].position)) < 1e9f; bool originFlag = origin; l1.position=pos; l1.LookAt(right?cp[next].position:cp[prev].position); l1.localPosition += l1.forward*distanceToTurn; l1.forward = originFlag ? cp[prev].normal : cp[currentIndex].normal; l2.position=pos; l2.LookAt(right?cp[prev].position:cp[next].position); l2.localPosition += l2.forward*distanceToTurn; l2.forward = savedNormal; Vector3 inter; bool ok = ProjectorMovement.LineLineIntersection(out inter, l1.position, l1.forward, l2.position, l2.forward); if(!ok || float.IsNaN(inter.x) || Vector3.Distance(inter,pos)>50f) inter = pos; return inter; }; var sb = new System.Text.StringBuilder(); for(int c=0;c<N;c++){ int prev=(c-1>-1)?c-1:N-1; int next=(c+1<N)?c+1:0; Vector3 dirToNext=(cp[next].position-cp[c].position).normalized; Vector3 dirToPrev=(cp[prev].position-cp[c].position).normalized; float segPrev=Vector3.Distance(cp[c].position,cp[prev].position); float segNext=Vector3.Distance(cp[c].position,cp[next].position); Vector3 decalR = cp[c].position + dirToPrev*distanceToTurn; Vector3 savedR = cp[prev].normal; Vector3 pivotR = getPivot(c,true,decalR,savedR,Vector3.zero); float swingR = Vector3.Distance(decalR,pivotR); Vector3 decalL = cp[c].position + dirToNext*distanceToTurn; Vector3 savedL = cp[c].normal; Vector3 pivotL = getPivot(c,false,decalL,savedL,Vector3.zero); float swingL = Vector3.Distance(decalL,pivotL); sb.Append(\"C\"+c+\" segPrev=\"+segPrev.ToString(\"F2\")+\" segNext=\"+segNext.ToString(\"F2\")+\" swingR=\"+swingR.ToString(\"F2\")+\" swingL=\"+swingL.ToString(\"F2\")+\"\\n\"); } GameObject.DestroyImmediate(l1.gameObject); GameObject.DestroyImmediate(l2.gameObject); rs.cornerPoints.Clear(); rs.meshPoints.Clear(); return new { count=N, table=sb.ToString() };"
}
